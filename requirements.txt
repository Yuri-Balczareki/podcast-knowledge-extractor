# =============================================================================
# Podcast Knowledge Extractor — Python Dependencies
# Python 3.12+ required
# =============================================================================

# --- Phase 1: Ingestion ---
feedparser>=6.0
httpx[http2]>=0.27
tqdm>=4.66

# --- Phase 2: Transcription ---
# OpenAI Whisper (original reference implementation)
openai-whisper>=20240930
# faster-whisper (CTranslate2-based, ~4x faster)
faster-whisper>=1.1
# whisper.cpp (GGML-based, native Metal GPU on Apple Silicon)
pywhispercpp>=1.4

# --- Phase 3: Speaker Diarization ---
# Pinned <4.0: v4.x requires torchcodec which needs system FFmpeg (brew install ffmpeg),
# broken on this machine. v3.x uses torchaudio instead.
pyannote.audio>=3.3,<4.0
soundfile>=0.13
# Pinned to stay compatible with pyannote.audio 3.x
huggingface_hub>=0.19,<0.24
torch>=2.5,<2.6
torchaudio>=2.5,<2.6

# --- Phase 4: Indexing & Vector Store ---
chromadb>=0.5
sentence-transformers>=3.3
rank-bm25>=0.2

# --- Phase 5: Q&A Interface ---
langchain>=0.3
langchain-community>=0.3
langchain-ollama>=0.3
streamlit>=1.40

# --- Benchmarking ---
jiwer>=3.0

# --- Dev Tools ---
pytest
ruff
python-dotenv
